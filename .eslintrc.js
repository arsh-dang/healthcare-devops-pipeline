module.exports = {
  extends: ['react-app', 'react-app/jest'],
  // ESLint 9 removed these options, but we're now using ESLint 8 which supports them 
  // if they are needed by any plugins or configurations
  rules: {
    // Add any custom rules here
  }
};